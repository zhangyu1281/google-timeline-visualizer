{
  "installCommand": "pnpm install --frozen-lockfile && cd web && pnpm install --frozen-lockfile",
  "buildCommand": "cd web && pnpm test && pnpm build",
  "outputDirectory": "web/dist",
  "framework": null,
  "redirects": [
    {
      "source": "/:path*",
      "has": [{ "type": "host", "value": "timelinevisualizer.app" }],
      "destination": "https://www.timelinevisualizer.app/:path*",
      "permanent": true
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
